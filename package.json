{
  "name": "chadepo-web",
  "version": "1.0.0",
  "scripts": {
    "build": "cd admin-src && npm install && npm run build",
    "start": "serve . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
